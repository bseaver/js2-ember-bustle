{{#if updateArticleForm}}
  <h4>Update {{article.headline}}</h4>
  <div>
    <form>
      <div class="form-group">
        <label for="category">Category</label>
        {{input value=category id="category" placeholder=article.category}}
      </div>

      <div class="form-group">
        <label for="headline">Headline</label>
        {{input value=headline id="headline" placeholder=article.headline}}
      </div>

      <div class="form-group">
        <label for="byline">Byline</label>
        {{input value=byline id="byline" placeholder=article.byline}}
      </div>

      <div class="form-group">
        <label for="story">Story</label>
        {{input value=story id="story" placeholder=article.story}}
      </div>

      <div class="form-group">
        <label for="release">Release Date</label>
        {{input value=release id="release" placeholder=article.release}}
      </div>

      <div class="form-group">
        <label for="picture">Image URL</label>
        {{input value=picture id="picture" placeholder=article.picture}}
      </div>

      <button {{action 'update' article}}>Save</button>
    </form>
  </div>
{{else}}
  <button {{action 'updateArticleForm'}}>Update</button>
{{/if}}
